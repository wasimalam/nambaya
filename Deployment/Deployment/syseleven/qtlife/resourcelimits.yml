apiVersion: v1
kind: LimitRange
metadata:
  name: cpu-limit-range
spec:
  limits:
  - default:
      cpu: 150m
      memory: 256Mi
    defaultRequest:
      cpu: 50m
      memory: 100Mi
    type: Container